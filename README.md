# Python_challenge
within the file contains both python scripts for Pybank as well as PyPoll.
As well as a folder within in .txt file when the code is run output to the "Activity" folder
The csv files are places within the Resorces files
All items where pushed to github using the terminal 
